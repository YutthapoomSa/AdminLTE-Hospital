import {DateFormat1Pipe} from './DateFormat1Pipe';

describe('DateFormat1Pipe', () => {
    it('create an instance', () => {
        const pipe = new DateFormat1Pipe();
        expect(pipe).toBeTruthy();
    });
});
