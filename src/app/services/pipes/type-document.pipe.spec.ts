import {TypeDocumentPipe} from './type-document.pipe';

describe('TypeDocumentPipe', () => {
    it('create an instance', () => {
        const pipe = new TypeDocumentPipe();
        expect(pipe).toBeTruthy();
    });
});
